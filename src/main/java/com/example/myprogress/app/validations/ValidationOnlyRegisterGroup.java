package com.example.myprogress.app.validations;

// This interface represents a group of classes that will be validated only when this group is called, then all classes belonging to this group will be validated each with its own validation implemented by its own @interface
public interface ValidationOnlyRegisterGroup {

}
